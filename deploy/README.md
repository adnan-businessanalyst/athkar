# Phase 3 — API stack

Compose services: Postgres 16, Redis 7, FastAPI on `127.0.0.1:8080`.

Public host: `https://api.athkar.ghurabi.com`  
Health: `GET /health` → `{"ok":true}`

See the chat reply for the exact commands to run on the VPS.
